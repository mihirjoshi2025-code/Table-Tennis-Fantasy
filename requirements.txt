# Table-Tennis-Fantasy backend
# Simulation engine uses only stdlib; tests use pytest.
pytest>=7.0.0

# API layer
fastapi>=0.109.0
uvicorn>=0.27.0
httpx>=0.27.0  # for TestClient

# Explanation feature (LLM). Requires OPENAI_API_KEY for POST /explain/match.
openai>=1.0.0
